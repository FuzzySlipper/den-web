export type WorkspaceViewMode = 'tasks' | 'messages' | 'documents' | 'git' | 'librarian' | 'agent-stream' | 'sessions' | 'agents' | 'dm' | 'pi-crew-diagnostics' | 'notifications';

/** Order of main panel modes for the FilterBar toggle and navigation hotkeys. */
export const WORKSPACE_VIEW_MODES: WorkspaceViewMode[] = ['tasks', 'messages', 'documents', 'git', 'librarian', 'agent-stream', 'sessions', 'agents', 'dm', 'pi-crew-diagnostics', 'notifications'];
